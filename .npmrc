# Hoist all dependencies to the root node_modules so the IDE and TypeScript
# always resolve libs (axios, firebase, react, next, etc.) no matter which
# tsconfig is used. Fixes "Cannot find module" in packages without running
# pnpm install every time.
public-hoist-pattern[]=*
