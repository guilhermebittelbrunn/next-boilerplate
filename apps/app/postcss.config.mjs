/** biome-ignore-all lint/performance/noBarrelFile: o PostCSS carrega este arquivo pela convenção de nome na raiz do app; ele existe só para apontar para a config compartilhada do design system. */
export { default } from "@repo/design-system/postcss.config.mjs";
