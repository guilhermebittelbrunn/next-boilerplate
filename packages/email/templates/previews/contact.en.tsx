import { contactPreviewData } from "../../preview-data";
import ContactEmail from "../contact";

const ContactEmailEn = () => (
    <ContactEmail data={contactPreviewData} locale="en" />
);

export default ContactEmailEn;
