import { contactPreviewData } from "../../preview-data";
import ContactEmail from "../contact";

const ContactEmailEs = () => (
    <ContactEmail data={contactPreviewData} locale="es" />
);

export default ContactEmailEs;
